{
    "config": "dev",
    "logger": {
        "syslog": { "level": "debug", "transport": ["console"]},
        "audit": { "level": "A", "transport": ["console"]}
    },
    "baboonDb" : {
        "dbName": "baboon",
        "dbOptions": {"w": 0},
        "server": [
            {"host": "localhost", "port": 27017, "options": { "auto_reconnect": true } }
        ]
    }
}
