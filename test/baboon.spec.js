'use strict';

describe('Baboon', function () {

    var path = require('path');
    var baboon = require(path.resolve(__dirname, '../', 'lib', 'baboon'));

    it('replace this generated placeholder', function() {
        expect(baboon).toBeTruthy();
    });
});
