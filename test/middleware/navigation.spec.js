'use strict';

describe('Middleware/Navigation', function () {

    var path = require('path');
    var navigation = require(path.resolve(__dirname, '../../', 'lib', 'middleware', 'navigation'));

    it('replace this generated placeholder', function() {
        expect(navigation).toBeTruthy();
    });
});
