'use strict';

describe('Server', function () {

    //var path = require('path');
    //var server = require(path.resolve(__dirname, '../../', 'lib', 'middleware', 'navigation'));

    var server = {};

    it('replace this generated placeholder', function() {
        expect(server).toBeTruthy();
    });
});
